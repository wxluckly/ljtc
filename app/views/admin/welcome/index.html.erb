数据统计

<div class="row">
  <div class="span12">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>注册用户数</th>
          <th>总攻略篇数</th>
          <th>总攻略的人数</th>
          <th>有效攻略篇数</th>
          <th>有效攻略的人数</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= User.count %></td>
          <td><%= Travel.count %></td>
          <td><%= Travel.all.collect(&:user_id).uniq.count %></td>
          <td><%= Travel.where(event_id: nil).where(is_blocked: 0).count %></td>
          <td><%= Travel.where(event_id: nil).where(is_blocked: 0).collect(&:user_id).uniq.count %></td>
        </tr>
      </tbody>
    </table>
  </div>
</div>